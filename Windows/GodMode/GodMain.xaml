<Window x:Class="OpenCardMaker.Windows.GodMode.GodMain"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:OpenCardMaker.Windows.GodMode"
        mc:Ignorable="d"
        Title="OpenCardMaker (God Mode)" Height="450" Width="600" SizeToContent="WidthAndHeight" WindowStartupLocation="CenterScreen" WindowState="Normal">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="350"/>
            <RowDefinition/>
        </Grid.RowDefinitions>
        <TabControl Margin="5,5,5,5">
            <TabItem Header="Card List">
                <Grid Background="#FFE5E5E5">
                    <DataGrid Name="UserCardListData" AlternatingRowBackground="LightGray" AlternationCount="2" AutoGenerateColumns="False">
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="ID" Binding="{Binding CardId}" Width="60" />
                            <DataGridTextColumn Header="Name" Binding="{Binding CardName}" Width="100" />
                            <DataGridTextColumn Header="Card Title" Binding="{Binding CardTitle}" Width="150"/>
                            <DataGridTextColumn Header="Level" Binding="{Binding CardLevel}" Width="40"/>
                            <DataGridTextColumn Header="Skill" Binding="{Binding CardSkill}" Width="150"/>
                        </DataGrid.Columns>
                    </DataGrid>
                </Grid>
            </TabItem>
            <TabItem Header="User Settings">
                <Grid Background="#FFE5E5E5">
                    <TextBlock HorizontalAlignment="Left" Margin="273,0,0,0" Text="WIP" TextWrapping="Wrap" VerticalAlignment="Center"/>
                </Grid>
            </TabItem>
        </TabControl>
        <TextBlock Name="UserName" HorizontalAlignment="Right" Grid.Row="1" Text="UserId: UserName" TextWrapping="Wrap" VerticalAlignment="Top" Margin="0,7,75,0"/>
        <Button Content="Logout" HorizontalAlignment="Right" Grid.Row="1" VerticalAlignment="Top" Height="20" Width="60" Margin="0,5,5,0"/>
    </Grid>
</Window>
